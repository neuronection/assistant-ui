---
'@neuronection/assistant-ui': minor
---

AiActionsDropdown: split-button mode via `primaryAction` — the main segment runs the primary action directly, a chevron opens the panel with the remaining actions (primary excluded, name via `moreLabel`).
