---
'@neuronection/assistant-ui': minor
---

ChatToolsCatalog: optional `badge` chip on catalog entries (`{ label, tone?: 'info' | 'warning' }`) rendered beside the tool name — lets apps mark non-callable HITL capability rows distinctly from callable tools (family ADR-0015; warning tone mirrors the HitlProposalCard accent tokens).
