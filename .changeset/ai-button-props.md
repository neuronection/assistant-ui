---
'@neuronection/assistant-ui': patch
---

`AiButton`: new `showLabel` (icon-only trigger), `closeOnSubmit`
(auto-close after submit) and controlled `open`/`onOpenChange` props
(close-on-success form-fill flows).
