---
'@neuronection/assistant-ui': patch
---

`TaskAssignmentPicker` info buttons are always visible and their popups open on click only — no hover-triggered description popups while moving across the rows.
