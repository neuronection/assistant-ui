---
'@neuronection/assistant-ui': minor
---

SettingsShell nav items accept an optional `trailing` node rendered at the
row's trailing edge — for status dots, counts or badges next to the label.
SettingsShell also gains a `navTestId` prop (data-testid on the `<nav>`)
for app test suites.
